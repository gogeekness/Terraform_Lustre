# Squid ACL Rules
acl localnet src 10.0.1.0/24       # Allow your internal subnet
http_access allow localnet
http_access allow localhost

# Deny everything else
http_access deny all

# HTTP port (default 3128)
http_port 3128